# Database URL (SQLite default). To use Postgres, replace with a Postgres connection string.
DATABASE_URL="file:./dev.db"

# AI provider key (optional). If not set, AI endpoints return canned suggestions.
AI_API_KEY=""
